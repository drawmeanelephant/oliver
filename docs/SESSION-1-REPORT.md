---
published_at: 2026-08-14T00:00:00Z
summary: "The first Oliver session: repository foundation, specification research, feature matrix, document model, and the smallest vertical slice."
---

# Session 1 Report

The first Oliver session: repository foundation, specification research,
feature matrix, document model, and the smallest vertical slice.

## Files created

```text
build.zig                   Zig 0.16 build: static lib, CLI, two test suites
build.zig.zon               package manifest (fingerprint generated by zig)
README.md                   overview
LICENSE                     (pre-existing, MIT)
src/
  oliver.zig                public API root: parse(), ParseResult, Dialect
  source.zig                Source, Span (u32), line iterator, line/column
  document.zig              Document, Node, Tag, Data, deterministic iterator
  diagnostic.zig            structured diagnostics (severity, code, offsets...)
  markdown.zig              Markdown frontend: paragraphs, ATX headings, text
  textile.zig               Textile frontend: paragraphs, hN. headings, text,
                            hard line breaks
  html.zig                  deterministic HTML renderer (explicit-stack walk)
  main.zig                  provisional CLI (args + stdio only)
tests/
  fixtures_test.zig         fixture runner, shared-model proof, adversarial
                            smoke, NUL policy
  fixtures/markdown/*.md|html   12 fixtures
  fixtures/textile/*.textile|html  10 fixtures
docs/
  CLEANROOM.md              clean-room contract + sources consulted
  ARCHITECTURE.md           pipeline, ownership, HTML policy, WASM posture
  FEATURE-MATRIX.md         Markdown/Textile matrix + recorded ambiguities
  DOCUMENT-MODEL.md         normalized model design + growth path
  TESTS.md                  fixture and test conventions
  SESSION-1-REPORT.md       this report
```

## Architecture chosen

Single-pass line-based block parsing per frontend, then inline parsing
(block-then-inline matches the CommonMark precedence model). Both frontends
build the same arena-backed `Document`; one renderer consumes it. Node text
payloads are borrowed slices of the input. Rendering walks the tree with an
explicit stack. Offsets are u32 with a documented 4 GiB input bound.

Module names track the mission's suggestions (`source`, `document`,
`markdown`, `textile`, `html`, `diagnostic`) — `span` lives inside
`source.zig`; `node` inside `document.zig`, since each pair is one cohesive
concept.

## Clean-room sources consulted

Only specifications and user-facing documentation (full list with URLs in
[docs/CLEANROOM.md](CLEANROOM.html)):

- CommonMark specification 0.31.2 `spec.txt` (lines, blank lines, tabs, NUL,
  backslash escapes, ATX headings §4.2, setext §4.3, precedence).
- Hobix "Textile Reference" (Dean Allen) — user-facing syntax.
- Movable Type "Textile 2 Syntax" (Brad Choate) — user-facing syntax.
- Zig 0.16 standard library (official library documentation) for the new
  `std.Io` / `std.process.Init` APIs.

No parser implementation source was consulted.

## Feature matrix

Full matrix in [docs/FEATURE-MATRIX.md](FEATURE-MATRIX.html).
Implemented in the slice: paragraphs, ATX headings, plain text, soft/hard
breaks, blank lines, Textile `p.`/`hN.` markers, Textile hard line breaks.
Planned next: emphasis/strong, code spans, links, escapes, thematic breaks,
lists, block quotes, code blocks, Textile phrase modifiers. Deferred:
entities, Setext, indented/fenced code, tables, Textile attributes,
character replacements, reference link definitions.

Nine ambiguities were recorded with chosen behaviors, including Textile
line-break policy (Hobix example vs prose vs Textile 2), Textile block
interruption without blank lines, marker whitespace rules, Markdown ATX
closing-sequence single-pass behavior, tab indentation, and NUL timing.

## Public API shape (provisional)

```zig
const result = try oliver.parse(allocator, source, .markdown, .{});
defer result.deinit();
try oliver.html.render(allocator, &writer, &result.document, .{});
```

`ParseResult { document, diagnostics }`; `Dialect { markdown, textile }`;
`ParseOptions` (empty); `ParseError { InputTooLarge, OutOfMemory }`. The
renderer takes a pointer to any `writeAll`-capable writer (Zig 0.16
`std.Io.Writer` values), so it streams to memory or a file without reparsing.

## Memory ownership model

Caller supplies the allocator. `Document` owns an `ArenaAllocator`; all nodes
and diagnostics live in it; `deinit()` frees everything at once. Text is
borrowed from the input (must outlive the result) — never copied. The
renderer allocates only a temporary traversal stack. No globals, no caches.

## Tests and results

`zig build test`: **20/20 passing** (14 module tests + 6 fixture tests), run
under `std.testing.allocator` (leak-checked).

- 12 Markdown fixtures + 10 Textile fixtures, byte-exact expected HTML.
- Shared-model proof: `# Hello` (MD) and `h1. Hello` (Textile) render
  byte-identically through the one renderer; same for paragraphs and
  heading+paragraph documents.
- Direct hand-built-document renderer tests (escaping, `<br />` policy,
  heading clamp, empty document).
- Source-span assertions (heading line span, trimmed text span).
- Adversarial smoke: empty input, blank runs, huge `#` runs, NUL bytes,
  mixed line endings, 100 KB lines, through both dialects — no crash/leak.
- CLI verified end to end: `printf '# Hello' | oliver render --from
  markdown` → `<h1>Hello</h1>`, CRLF input normalizes to LF output, bad
  flags exit 1 with usage.

## Known ambiguities (summary)

1. Textile line breaks (`<br />` chosen). 2. Textile marker interruption
   without blank line (chosen: markers interrupt). 3. Marker must be followed
   by space/tab (chosen). 4. Paragraph whitespace: Textile verbatim vs
   Markdown trimmed. 5. Textile inline modifier sets differ between the two
   references. 6. ATX closing sequence in `# foo # #` (single pass chosen).
   7. Tab indentation (leading tab disqualifies ATX; full tab stops
   deferred). 8. Paragraph line trimming provisional vs CommonMark §4.8.
   9. NUL replacement at render time vs parse time (output identical).

## Deliberately deferred

Everything outside the slice: all other blocks and inlines, entities,
attributes, HTML blocks/inline HTML (needs a raw-HTML policy decision),
tables, Setext, reference definitions, character replacements, the C ABI,
real CLI features (file args, --help polish), fuzz target, benchmark
corpora, WebAssembly target testing, and all of Phases 8–11. Nothing was
half-built; the matrix marks each.

## Next smallest implementation slice

**Markdown inline milestone, part 1: emphasis/strong + backslash escapes +
hard breaks**, because it forces the delimiter algorithm (the hardest open
design question) while the block layer is still small, and Textile's
`_em_`/`*strong*` can ride the same inline seam immediately. Suggested order:
(1) backslash escapes (§2.2) + trailing-space hard breaks (§6.7) with
fixtures; (2) write docs/INLINE-PARSING.md from §6.4 alone; (3) implement
emphasis/strong with source-span tests; (4) add Textile `_`/`*`/`**`/`__`
mapping to the same nodes.

## Addendum: Markdown inline milestone (after session 1)

**Delivered:** backslash escapes (§2.4), hard breaks (§6.7 / two+ trailing
spaces or unescaped trailing backslash), escape-aware ATX closing sequences
(§4.2 example 76), and the deliberate paragraph-whitespace policy change the
session report called for.

- `src/markdown.zig` now stores raw per-line spans; the inline pass skips
  leading whitespace, resolves escapes into adjacent text nodes with exact
  spans, and consumes trailing whitespace according to the break rule.
  `analyzeLineEnd` decides soft vs hard break; `emitTextRuns` handles escapes.
- ATX closing-sequence detection is escape-aware (spec example 76 passes
  byte-for-byte), and `# foo\` renders `<h1>foo<br />\n</h1>` (chosen
  behavior, recorded in the matrix as low-confidence pending conformance).
- 10 new Markdown fixtures (22 Markdown total): the full punctuation-escape
  example, non-punctuation literal backslashes, escaped backslashes, both
  hard-break forms, single-space soft break, last-line trailing spaces,
  escape-aware closing sequences, escaped structural characters, indented
  continuation lines. Tests: 25/25 (19 module + 6 fixture), all under the
  leak-checking allocator; CLI verified against spec examples 12, 16, 76.
- The open question recorded in the matrix: whether CommonMark preserves
  interior leading whitespace on paragraph continuation lines in exact
  output (cannot be settled until §4.4 indented code blocks land).
- Next slice per the original plan: the written delimiter algorithm
  (docs/INLINE-PARSING.md) and emphasis/strong.

## Addendum: emphasis/strong milestone

**Delivered:** the full CommonMark §6.2 emphasis/strong rule set, per the
design contract in docs/INLINE-PARSING.md.

- `src/unicode.zig` embeds generated Unicode 13.0 tables (Zs whitespace,
  P+S punctuation per §2.1) with a strict, non-panicking hand-rolled UTF-8
decoder (`std.unicode.utf8Decode` asserts on malformed lead bytes and
crashed the test suite; the replacement rejects overlongs and surrogates
and degrades malformed input to null).
- `src/markdown.zig` gained the three-phase inline pass: scan (flanking
  classification against the raw per-line span), match (delimiter stack +
  `openers_bottom` for amortized linear matching, mod-3 rule,
  strong-before-emphasis), emit (explicit-stack node materialization).
  Consumed delimiters are attributed from the opener's back and the closer's
  front (separate `front_used`/`back_used` counters — required for intraword
  runs that act as both closer and opener, e.g. `*a**b*c**`). Two real bugs
  were caught by the spec examples during implementation: an off-by-one in
  the emit-phase bucket prefix sums (every match bucket came out empty) and
  opener segments attributed from the wrong end of the run (broke
  `***foo** bar*` nesting).
- The normalized model now merges contiguous text nodes at emission
  (invariant 7 in docs/DOCUMENT-MODEL.md): scanning artifacts no longer
  fragment text, so `foo_bar_baz` is one node while `a\*b` stays two (the
  consumed backslash leaves a span gap). Two earlier tests and one fixture
  were corrected for this (the `\*literal*` fixture now correctly parses
  `*literal*` as emphasis after the `\\` escape pair).
- 19 new Markdown fixtures (40 Markdown total) covering §15's corpus
  (simplest, intraword, whitespace, mod-3, malformed, escapes, breaks,
  headings, Unicode incl. full-width punctuation and emoji-as-symbol
  flanking); renderer-only hand-built emphasis/strong test; adversarial
  smoke extended with 100 KB `*` runs, a 10k-deep open chain, and 50k
  alternating `*`/`_` runs (completion in ~350 ms, no crash/leak).
  Verified against spec examples 384–418. Tests: 36/36.
- Chosen behaviors recorded: mod-3 uses *remaining* run lengths (the §16
  open question stays open until a distinguishing spec example appears),
  `openers_bottom` shared across closer kinds reproduces the reference
  `*****Hello*world****` output, and escaped-characters-as-punctuation for
  flanking remains the §16.1 chosen reading.
- Next slice: code spans (§6.6), which introduce the first
  delimiter-opacity rule (code span markers are opaque to delimiters), or
  Textile inline mapping (`_`/`*`/`**`/`__`) onto the same seam.

## Addendum: code spans (session 3)

- **Implemented §6.6** on the same scan → match → emit seam. Backtick-string
  discovery runs ahead of delimiter scanning (per-line, but spans may cross
  lines), so code span content is opaque to `*`/`_` before flanking is
  classified — the delimiter-opacity precedence from §6.1's "code spans have
  higher precedence than any other inline constructs."
- **Run-length matching:** an opener closes at the next backtick string of
  equal length; `2-against-1` does not match, longer content runs are kept,
  unmatched backticks stay literal. **Normalization:** line endings become
  spaces; one ASCII space stripped per side when the content begins *and*
  ends with space but is not all spaces (tabs are never stripped, spec 344's
  note); backslashes are inert and the closer scan ignores them (spec 340,
  `` `foo\`bar` `` → `<code>foo\</code>bar`).
- **Model/renderer:** `code_span` is a leaf inline; `data.code_span` is the
  one arena-owned payload (normalized copy — the only inline whose content
  is not a borrowed source slice), and the node span covers the whole
  construct including backticks. Renderer escapes content as text
  (renderer-only test added).
- 12 new Markdown fixtures (52 Markdown total): simple/empty, run-length
  matching, trim rule, multiline, escape inertness, delimiter opacity,
  emphasis wrapping, headings, Unicode, unmatched runs, and mixed blocks.
  Verified against spec examples 333–348 (E17–E31 shapes; HTML-tag and
  autolink precedence examples defer with those features). Six new unit
  tests pin run-length, trim, newline, opacity, and span semantics.
  Adversarial smoke extended with 100 KB backtick runs and 50k
  alternating `` ` ``/`*` runs. Tests: 42/42.
- **Also fixed:** a latent const-qualifier bug in html.zig's pre-existing
  renderer test (`const out2` + `deinit` needs `*Self`), which would have
  broken compilation of html.zig's tests standalone; html.zig now compiles
  and runs its 10 tests (6 renderer + transitively imported suites).
- Next slice: links (§6.5) — the other §6.1 precedence (link brackets are
  opaque to emphasis) — or Textile inline mapping onto the same seam.

## Addendum: inline links (session 4)

- **Implemented §6.6 (inline links)** on the same scan → match → emit seam
  as a second discovery pass with its own bracket stack. Unescaped `[`/`]`
  become scan items; on a `]` whose nearest `[` is followed by a valid
  `(...)` in the source, the bracket range plus the consumed `(...)` items
  splice into a single `link` item (children = link-text items), and every
  earlier `[` dies — links cannot contain links, innermost wins (the spec's
  bracket-stack algorithm, §Appendix). Brackets that never form a link stay
  literal text. Link text is matched as a **fresh inline scope** (the
  spec's "process emphasis with the `[` opener as stack_bottom"): emphasis
  and code spans inside link text work; delimiters outside never reach in.
- **Precedence verified:** `*[foo*](/uri)` is a link (brackets bind tighter
  than emphasis), `` [foo`](/uri)` `` keeps the code span (code spans bind
  tighter than brackets), `[foo [bar](/uri)](/uri)` leaves the outer
  brackets literal, `[link [foo [bar]]](/uri)` keeps balanced inner
  brackets as text. Verified byte-for-byte against spec examples 482–536
  (inline subset), including the dest/title forms (`<...>`, bare with
  balanced/escaped parens, `"`/`'`/`(...)` titles, one-line-ending
  separators, no line endings inside destinations, multiline titles).
- **Model/renderer:** `data.link` is arena-owned `{ href, title }` with
  backslash escapes resolved at parse time (the renderer never resolves
  dialect escapes). The renderer percent-encodes `href` per a documented
  policy derived from spec examples (encode everything except alphanumerics
  and `-_.~!*'(),;:&=+$#@/%?`), then HTML-escapes; `title` is
  HTML-escaped without percent-encoding; attributes emit in fixed order
  `href` then `title`. `![alt](url)` currently parses as `!` + link
  (images deferred — documented divergence).
- **DoS guards:** the smoke test caught a quadratic blowup on `[a](`
  repeated (every `]` rescanned the rest of the line). Fixed with a paren
  depth cap of 32 — explicitly sanctioned by the spec ("implementations may
  impose limits on parentheses nesting... at least three levels should be
  supported") — and a 2048-byte per-component scan cap. Stress shapes now
  complete in ~0.02–0.2 s (were ~4 s).
- 22 new Markdown fixtures (74 Markdown total; one of them locks the
  entity divergence — `&ouml;` stays literal in destinations/titles until
  the entity milestone), 11 new unit tests, 1 renderer-only link test.
  Tests: 53/53; `zig fmt --check` clean; build clean.
- Next slice: reference links (§4.7 link reference definitions, needed for
  `[text][label]` and `[text][]` forms) or images (§6.7) or Textile inline
  mapping onto the same seam.

## Addendum: reference links (session 6)

- **Two-phase restructure.** Link reference definitions (§4.7) must be
  known before inline parsing (a use may precede its definition), so
  `parse` now runs the block pass to completion collecting definitions
  (into a case-insensitive map) and *deferring* inline parsing, then
  resolves inlines in phase two with the definitions map in hand.
  Definitions are extracted at paragraph close: the first line must be
  `[` + label + `]` + optional spaces/tabs + destination; a title may
  follow on the same line (spaces/tabs separated) or on the *next* line;
  nothing else may follow a title on its line — a failed title attempt
  ends the definition at the destination's line (spec example
  `[foo]: /url
"title" ok` is definition + paragraph). A paragraph whose
  content is entirely definitions renders nothing; a definition inside a
  paragraph of real content is consumed without affecting the rest.
- **Label matching (§6.3):** labels are normalized by Unicode full
  case-fold plus whitespace collapse. The case-fold tables (single
  codepoints + the 104 multi-codepoint expansions, e.g. `ẞ` → `ss`) are
  generated by `tools/gen-unicode.py` from the Unicode standard
  (`str.casefold`, Unicode 13.0.0 — the same data the spec references)
  into `src/unicode.zig`; `foldInto` binary-searches then expands.
  First definition wins; an unclosed `[` can never be a definition;
  definitions cannot interrupt paragraphs (they must be a continuation
  of content or start at a blank-line boundary).
- **Reference forms (§6.6):** at each `]` in discovery, after a failed
  inline `(...)`, resolution tries full `[text][label]` then collapsed
  `[text][]` then shortcut `[text]` (the label is the link text itself),
  each via the definitions map. Per spec example 561, a `(` after the `]`
  that fails to parse as an inline destination does *not* block the
  shortcut form. Reference labels cannot contain other reference links
  (the label is matched raw from the source, not as inlines).
  Reference links produce the same `link` node as inline links — no
  model or renderer change.
- **Verified** byte-for-byte against the §4.7 and §6.6 reference-link
  examples (full/collapsed/shortcut, Unicode and case-insensitive
  labels, whitespace normalization, first-wins, use-before-definition,
  multiline titles, cannot-interrupt, escaped labels, and the
  `[foo](not a link)` fall-through), plus 24 new Markdown fixtures.
- **Smoke additions:** 50k shortcut label bombs, 50k near-miss labels
  (differ by one character), 30k collapsed forms, 20k failed-inline
  fall-throughs, a 20k-definition storm with interleaved uses, and a
  200 KB label against the definitions map — all linear, suite still
  runs in well under a second.
- 115 Markdown fixtures (24 new on top of the images milestone's 91),
  62 library tests, 68/68 total; `zig fmt --check` clean; build clean.

## Addendum: autolinks (session 8)

- **Implemented §6.8 (autolinks)** on the same scan → match → emit seam,
  per the design contract docs/AUTOLINKS.md (written from the spec and
  committed before any autolink code). A `<` is a recognizer in the
  scan: URI autolinks (scheme 2–32 chars followed by `:`, then content
  up to the first `>`; content forbids ASCII control, space, and `<`)
  and email autolinks (the non-normative HTML5 email regex, anchored),
  tried in that order. Recognition runs ahead of bracket discovery, so
  an autolink swallows what looks like link syntax (spec example 526:
  `<https://example.com/?search=](uri)>` keeps `](uri)` in its href),
  and code spans bind more tightly than `<` as before.
- **Model/renderer:** `.autolink` joined the leaf inlines — `data.autolink
  = { href, label }` — and DOCUMENT-MODEL invariants 4/5/8 were updated
  (it is the first leaf inline whose payload is *not* escape-resolved:
  backslash escapes are inert inside autolinks, so the label is the raw
  content verbatim, unlike `data.link`; the `mailto:` prefix makes the
  href an arena copy rather than a source slice). The renderer emits
  `<a href="...">label</a>`: href percent-encoded under the shared link
  policy (`&` → `&amp;`, `\` → `%5C`, `[` → `%5B`), label HTML-escaped
  like text.
- **One bug found during verification:** the content span initially
  excluded the scheme (the href/label must be the *whole* text between
  the angle brackets — `<m:abc>` fails only because the scheme is 1
  char, not because schemes are omitted from content). Fixed to span
  from after `<` to the `>`, inclusive of scheme and colon.
- **Verified** byte-for-byte against all 19 §6.8 spec examples 567–585
  via the spec-conformance harness (Autolinks 8/19 → 19/19), and the
  scorecard moved 360/652 → 377/652 overall with no regressions — the
  extra +6 come from autolinks now recognized inside link text (spec
  examples 526, 538), emphasis, code-span, and backslash-escape
  contexts in other sections.
- **Fixtures/tests:** 25 new `autolink-*` Markdown fixtures (153
  Markdown total; 19 spec-pure, byte-for-byte, plus composite shapes:
  in-sentence, in-emphasis, adjacent pairs, link-text precedence,
  uppercase email, image-alt flattening); 6 new unit tests (URI
  structure/spans/payloads, email `mailto:` hrefs, inert-escape
  payloads, literal negatives incl. the escape-resolved `\+` case,
  nesting inside emphasis/link text, alt flattening). Smoke extended
  with 30k URI, 20k email, 10k near-miss `<ab:...` scans, one 200 KB
  no-`>` content run, and a 20k mixed autolink/link/image/emphasis
  workload — all linear. 73 library tests, 79/79 total; `zig fmt
  --check` clean; build clean.
- Next slice: entities (§2.5 — unblocks several recorded divergences)
  or raw HTML (the `<` recognizer is the seam raw HTML must share;
  HTML blocks remain 0/44).

## Addendum: reference-style images (session 7)

- **Implemented the deferred image reference forms** (§6.4/§6.7) on top
  of the reference-links slice: `![alt][label]` (full), `![alt][]`
  (collapsed), and `![alt]` (shortcut) now resolve through the same §4.7
  definition table, label normalization, and full → collapsed → shortcut
  ordering as reference links, per the design contract
  docs/REFERENCE-IMAGES.md (written and committed before the code). The
  reference-links slice had gated the `!is_image` branches off in the
  shared discovery pass; this slice removes exactly that gate — the
  appendix's *look for link or image* procedure is uniform for links and
  images. No new model or renderer surface: a reference-style image is
  the same `.image` node, the definition supplies src/title, and the
  description flattens to the arena-owned `alt` via the existing
  `flattenAlt` path.
- **Correctness detail:** the collapsed/shortcut label is the opener's
  own bracket text, which for a `![` opener starts *after the bang* —
  the reference-links slice's `text` span (opener + 1) would have
  included the `[` byte for images, so this slice fixes the span start
  to `opener + 2` when the opener is an image. Full references were
  unaffected (their label is scanned independently).
- **Inactive-bracket rule unchanged:** a formed reference *link*
  inactivates earlier `[` openers via the same monotone O(1) check; a
  formed reference *image* inactivates nothing (matching the inline-image
  behavior).
- **Verified** byte-for-byte against the §6.4 reference-image spec
  examples 582–591 (full/collapsed/shortcut, case-insensitive labels,
  emphasis in the description flattening to alt, no-space-between-
  brackets, unescaped-bracket labels staying literal), plus 13 new
  Markdown fixtures (`ref-image-*`) covering those shapes, image inside
  reference-link text, inline-beats-reference precedence, first-wins,
  unmatched → literal, and definition-after-use.
- **Tests:** 5 new unit tests (full/collapsed/shortcut resolution,
  alt flattening through the reference forms, precedence and the
  exclusive full-reference path, image-in-reference-link, unmatched and
  the inactive-bracket shape); smoke extended with 30k `![alpha]`
  shortcut, 20k collapsed and 20k full reference-image bombs, and 30k
  near-miss labels — all linear. 128 Markdown fixtures (13 new), 67
  library tests, 73/73 total; `zig fmt --check` clean; build clean.
- **Also fixed:** a leftover merge-conflict marker in README.md's Status
  section that the reference-links merge (d76f971) had committed
  verbatim; resolved while updating the README.

## Addendum: inline images (session 5)

- **Implemented §6.7 (inline images)** on the same seam, per the design
  contract docs/IMAGES-PARSING.md (written from the spec and committed
  before any image code). `![` (unescaped `!` immediately followed by an
  unescaped `[`) is its own opener on the link discovery stack; the
  description is matched as a fresh inline scope and flattened to an
  arena-owned `alt` string ("only the plain string content"), so `.image`
  is a leaf inline with `data.image = { src, alt, title }`. `<img
  src="..." alt="..." title="..." />` renders as a void element under
  `void_trailing_slash`; `src` shares the href percent-encoding policy,
  `alt` is always emitted (possibly empty), `title` only when present,
  attribute order fixed `src`, `alt`, `title`.
- **The appendix's active/inactive bracket semantics**, faithfully: a
  formed *link* inactivates every earlier `[` opener, never a `![`, and an
  inactive `[` still intercepts a later `]` (a dead `[` above a live `![`
  must not let that `]` reach the image). Oliver implements the marker
  with a monotone O(1) out-position check instead of re-marking, so the
  adversarial dead-bracket shape stays linear. This fixes the documented
  divergence: `![foo](bar)` is now one `.image` node, not `!` + link.
- **Model/renderer:** `.image` joined the leaf inlines; invariants 4/5/8
  in docs/DOCUMENT-MODEL.md updated; the convergence table gained
  Markdown `![alt](url)` ↔ future Textile `!url(alt)!`. `Data.attrs`
  remains the reserved extension point for future image metadata
  (classes/ids/width-height/custom attributes) — no extension syntax was
  invented.
- 17 new Markdown fixtures (91 Markdown total): spec examples 572, 574,
  575, 578–581, and the literal forms of 592–593 verified byte-for-byte
  through the CLI before being locked, plus nested images, image-in-link
  (spec example 540), alt flattening (emphasis/code), the chosen
  break-to-`
` alt behavior, image in heading/emphasis, code-span
  precedence, unclosed → literal, and the inactive-bracket fixture. 6 new
  unit tests (image structure/spans/payloads, alt flattening, nesting,
  escape boundaries, precedence, escape-resolved payloads) and 1
  renderer-only image test; adversarial smoke extended with `![a](` bombs,
  deep `![` openers, an image workload mix, and the dead-bracket marking
  shape. Tests: 59/59.
- **Chosen behaviors recorded** (docs/IMAGES-PARSING.md §8 and the matrix
  ambiguities 12–14): soft/hard breaks flatten to `
`; code spans
  flatten to their §6.1-normalized content; empty description → `alt=""`;
  inactive brackets are kept and checked (not cleared); and the repo's
  §6.7 citation for images (the numbered 0.31.2 HTML says §6.4) is kept
  for continuity.
- Next slice: reference links (§4.7, which also enables reference-style
  images) or Textile inline mapping onto the same seam.


## Addendum: container blocks — block quotes (session 7)

- **Design-first.** docs/BLOCKS-PARSING.md was written from the spec
  (§5.1–5.3 plus the appendix's parsing strategy) before any container
  code: the full container architecture (block quotes, list items,
  lists, tight/loose) is specified, but only §5.1 ships here.
- **The container stack.** Phase 1 now keeps a stack of open containers
  plus one open leaf, replacing the flat single-paragraph loop: each line
  matches containers top-down consuming one `>` marker each (≤3 leading
  spaces, one following space), a blank line closes the leaf and any
  container whose marker was absent, a lazy continuation keeps unmatched
  containers open when the remainder is paragraph continuation text,
  and new block starts (block quote, ATX heading) interrupt the open
  paragraph. Definitions and deferred inlines are unchanged — phase-2
  jobs already carry their node, and definitions stay document-global.
- **Block quote semantics landed:** nesting (`> > foo`), laziness
  (including omitting any number of initial `>`s on nested continuation
  lines — spec example `>>> foo\n> bar\n>>baz`), marker-blank lines
  keeping one quote open vs a truly blank line separating two quotes,
  interruption (`foo\n> bar`), empty quotes (`>`), and definitions
  inside quotes registering document-wide. `block_quote` joined the
  model as a container (children: blocks; span covers stripped content)
  and the renderer gained `<blockquote>`/`</blockquote>\n`.
- **Verified:** `--section "Block quotes"` went 0/25 → 18/25; the 7
  remaining failures are exactly the pending constructs (indented code
  ×3, fenced code ×1, thematic breaks ×2, lists ×1), each with its
  milestone named in the design note. Full scorecard 348 → 370/655
  (the extra +4 are blockquotes inside §5.2 list-item examples). 18 new
  fixtures (17 byte-verified against spec examples), 7 new unit tests.
  Tests: 75/75; fmt clean; build clean.
- Next slices per the note: list items and lists (§5.2/§5.3) on the same
  stack, then thematic breaks (§4.1) and setext headings (§4.3) to close
  the lazy-continuation loop.

## Addendum: raw HTML (session 9)

- **Implemented CommonMark §6.6 raw HTML inline constructs** behind the `<`
  recognizer's natural extension: open/closing tags, comments, processing
  instructions, declarations, and CDATA sections are discovered across
  paragraph lines, merged with code spans for first-come precedence, and
  emitted as `.raw_html` leaves rendered verbatim from their source spans.
  HTML blocks remain a separate future milestone.
- The design contract in `docs/RAW-HTML.md` was committed first as
  `76b1c30`; the implementation followed in the next commit. Invalid tag
  shapes remain escaped text, custom names are accepted, and image-alt
  flattening uses the documented raw-source choice.
- 19 new Markdown fixture cases cover all 20 Raw HTML spec examples, with
  focused unit coverage for spans, code/tag precedence, multiline break
  suppression, image alt, and renderer verbatim output. Final local gate:
  84 library tests + 6 fixture tests = 90/90, with clean formatting, build,
  and diff checks.

## Architectural concerns discovered

1. **Zig 0.16's I/O redesign.** `std.io.AnyWriter` is gone; writers are
   vtable structs under `std.Io`, and `main()` now takes `std.process.Init`.
   The renderer's `anytype`-writer seam absorbed this cleanly, but any C-ABI
   or doc example must target the 0.16 shapes. The render-to-buffer path for
   the future C ABI should use `std.Io.Writer.Allocating`.
2. **The inline milestone will restructure `parseInlines`.** The slice's
   inline seam is a single text node per range; the delimiter algorithm will
   replace it. Block structure (the `Paragraph`/`AtxHeading` code) is
   expected to survive mostly intact, which is why the block layer was worth
   building first.
3. **Trailing-space policy must be decided before hard breaks.** Markdown
   hard breaks depend on trailing spaces, which the slice currently trims.
   The fixture `escape-special` and unit tests pin current behavior; the
   inline milestone must change trimming deliberately, with new fixtures.
4. **Textile `p.`/`hN.` content is preserved verbatim** while Markdown
   trims — a genuine dialect difference, now pinned by fixtures
   (`p-prefix`, `heading`), that must survive the inline milestone.
